package com.metacoding.blog.board;

// 반환한 문자열이 응답 본문이 아니라 화면(뷰) 이름이 되는 컨트롤러
public class BoardController {

    // TODO: 실습에서 화면 연결 메서드를 하나씩 작성한다.
    //  1) GET /                        → "index" 반환
    //  2) GET /boards/{id}             → "board/detail" 반환
    //  3) GET /boards/save-form        → "board/save-form" 반환
    //  4) GET /boards/{id}/update-form → "board/update-form" 반환
    //  각 메서드에 System.out.println 으로 요청 도달을 출력해 눈으로 확인한다.

}
